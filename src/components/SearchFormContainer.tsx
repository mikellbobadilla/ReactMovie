import { styled } from "@mui/material";

export const SearchFormContainer = styled('form')(() => ({
  display: 'flex',
  gap: '12px',
  alignItems: 'start',
  width: '100%',
  maxWidth: '600px',
  marginInline: 'auto'
}))